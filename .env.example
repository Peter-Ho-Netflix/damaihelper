# 数据库配置环境变量示例
# 本地开发环境使用以下配置
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=damaihelper

# 云服务器环境使用以下配置（优先级高于上面的配置）
# DATABASE_URL=postgresql://username:password@host:port/database

# 数据库调试模式
# DB_ECHO=true

# ChromeDriver配置
# CHROMEDRIVER_PATH=./chromedriver

# 代理配置
# PROXY_ENABLED=false
# PROXY_IP=127.0.0.1
# PROXY_PORT=7890

# 日志级别
# LOG_LEVEL=INFO

# FastAPI服务器配置
# SERVER_HOST=0.0.0.0
# SERVER_PORT=8000